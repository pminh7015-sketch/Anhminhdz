-- LocalScript
-- Đặt vào: StarterPlayer > StarterPlayerScripts

local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")

local player = Players.LocalPlayer

local hitboxEnabled = false
local noclipEnabled = false
local speedEnabled = false

local hitboxSize = 2
local speedValue = 16

local normalWalkSpeed = 16
local normalRootSize = Vector3.new(2, 2, 1)

-- GUI
local gui = Instance.new("ScreenGui")
gui.Name = "ControlMenu"
gui.ResetOnSpawn = false
gui.Parent = player:WaitForChild("PlayerGui")

local main = Instance.new("Frame")
main.Size = UDim2.fromOffset(280, 330)
main.Position = UDim2.new(0.5, -140, 0.5, -165)
main.BackgroundColor3 = Color3.fromRGB(30, 30, 35)
main.BorderSizePixel = 0
main.Active = true
main.Parent = gui

local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(0, 12)
corner.Parent = main

local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, 0, 0, 45)
title.BackgroundTransparency = 1
title.Text = "CONTROL MENU"
title.TextColor3 = Color3.new(1, 1, 1)
title.TextSize = 20
title.Font = Enum.Font.GothamBold
title.Parent = main

local function createButton(text, y)
	local b = Instance.new("TextButton")
	b.Size = UDim2.new(1, -30, 0, 40)
	b.Position = UDim2.fromOffset(15, y)
	b.BackgroundColor3 = Color3.fromRGB(170, 55, 55)
	b.Text = text
	b.TextColor3 = Color3.new(1, 1, 1)
	b.TextSize = 15
	b.Font = Enum.Font.GothamBold
	b.Parent = main

	local c = Instance.new("UICorner")
	c.CornerRadius = UDim.new(0, 8)
	c.Parent = b

	return b
end

local hitboxButton = createButton("HITBOX: TẮT", 50)
local noclipButton = createButton("XUYÊN TƯỜNG: TẮT", 95)
local speedButton = createButton("TỐC ĐỘ: TẮT", 140)

local hitboxLabel = Instance.new("TextLabel")
hitboxLabel.Size = UDim2.new(1, -30, 0, 25)
hitboxLabel.Position = UDim2.fromOffset(15, 190)
hitboxLabel.BackgroundTransparency = 1
hitboxLabel.Text = "Hitbox: 2 / 500"
hitboxLabel.TextColor3 = Color3.new(1, 1, 1)
hitboxLabel.TextSize = 14
hitboxLabel.Font = Enum.Font.Gotham
hitboxLabel.Parent = main

local hitboxBack = Instance.new("Frame")
hitboxBack.Size = UDim2.new(1, -30, 0, 12)
hitboxBack.Position = UDim2.fromOffset(15, 218)
hitboxBack.BackgroundColor3 = Color3.fromRGB(65, 65, 75)
hitboxBack.BorderSizePixel = 0
hitboxBack.Active = true
hitboxBack.Parent = main

local hitboxFill = Instance.new("Frame")
hitboxFill.Size = UDim2.new(0, 0, 1, 0)
hitboxFill.BackgroundColor3 = Color3.fromRGB(70, 170, 255)
hitboxFill.BorderSizePixel = 0
hitboxFill.Parent = hitboxBack

local speedLabel = Instance.new("TextLabel")
speedLabel.Size = UDim2.new(1, -30, 0, 25)
speedLabel.Position = UDim2.fromOffset(15, 240)
speedLabel.BackgroundTransparency = 1
speedLabel.Text = "Tốc độ: 16 / 100"
speedLabel.TextColor3 = Color3.new(1, 1, 1)
speedLabel.TextSize = 14
speedLabel.Font = Enum.Font.Gotham
speedLabel.Parent = main

local speedBack = Instance.new("Frame")
speedBack.Size = UDim2.new(1, -30, 0, 12)
speedBack.Position = UDim2.fromOffset(15, 268)
speedBack.BackgroundColor3 = Color3.fromRGB(65, 65, 75)
speedBack.BorderSizePixel = 0
speedBack.Active = true
speedBack.Parent = main

local speedFill = Instance.new("Frame")
speedFill.Size = UDim2.new(0.15, 0, 1, 0)
speedFill.BackgroundColor3 = Color3.fromRGB(255, 170, 60)
speedFill.BorderSizePixel = 0
speedFill.Parent = speedBack

local function getCharacter()
	return player.Character
end

local function getHumanoid()
	local c = getCharacter()
	if not c then return nil end
	return c:FindFirstChildOfClass("Humanoid")
end

local function getRoot()
	local c = getCharacter()
	if not c then return nil end
	return c:FindFirstChild("HumanoidRootPart")
end

local function applyHitbox()
	local root = getRoot()
	if not root then return end

	if hitboxEnabled then
		root.Size = Vector3.new(hitboxSize, hitboxSize, hitboxSize)
		root.Transparency = 0.5
	else
		root.Size = normalRootSize
		root.Transparency = 1
	end

	root.CanCollide = false
end

local function applySpeed()
	local humanoid = getHumanoid()
	if not humanoid then return end

	if speedEnabled then
		humanoid.WalkSpeed = speedValue
	else
		humanoid.WalkSpeed = normalWalkSpeed
	end
end

hitboxButton.MouseButton1Click:Connect(function()
	hitboxEnabled = not hitboxEnabled

	hitboxButton.Text = hitboxEnabled
		and "HITBOX: BẬT"
		or "HITBOX: TẮT"

	hitboxButton.BackgroundColor3 = hitboxEnabled
		and Color3.fromRGB(55, 170, 85)
		or Color3.fromRGB(170, 55, 55)

	applyHitbox()
end)

noclipButton.MouseButton1Click:Connect(function()
	noclipEnabled = not noclipEnabled

	noclipButton.Text = noclipEnabled
		and "XUYÊN TƯỜNG: BẬT"
		or "XUYÊN TƯỜNG: TẮT"

	noclipButton.BackgroundColor3 = noclipEnabled
		and Color3.fromRGB(55, 170, 85)
		or Color3.fromRGB(170, 55, 55)
end)

speedButton.MouseButton1Click:Connect(function()
	speedEnabled = not speedEnabled

	speedButton.Text = speedEnabled
		and "TỐC ĐỘ: BẬT"
		or "TỐC ĐỘ: TẮT"

	speedButton.BackgroundColor3 = speedEnabled
		and Color3.fromRGB(55, 170, 85)
		or Color3.fromRGB(170, 55, 55)

	applySpeed()
end)

local function setupSlider(back, fill, minimum, maximum, callback)
	local dragging = false

	local function update(x)
		local width = back.AbsoluteSize.X
		if width <= 0 then return end

		local percent = math.clamp(
			(x - back.AbsolutePosition.X) / width,
			0,
			1
		)

		local value = math.floor(
			minimum + (maximum - minimum) * percent + 0.5
		)

		fill.Size = UDim2.new(percent, 0, 1, 0)
		callback(value)
	end

	back.InputBegan:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1
			or input.UserInputType == Enum.UserInputType.Touch then

			dragging = true
			update(input.Position.X)
		end
	end)

	UserInputService.InputChanged:Connect(function(input)
		if not dragging then return end

		if input.UserInputType == Enum.UserInputType.MouseMovement
			or input.UserInputType == Enum.UserInputType.Touch then

			update(input.Position.X)
		end
	end)

	UserInputService.InputEnded:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1
			or input.UserInputType == Enum.UserInputType.Touch then

			dragging = false
		end
	end)
end

setupSlider(
	hitboxBack,
	hitboxFill,
	2,
	500,
	function(value)
		hitboxSize = value
		hitboxLabel.Text = "Hitbox: " .. value .. " / 500"
		applyHitbox()
	end
)

setupSlider(
	speedBack,
	speedFill,
	1,
	100,
	function(value)
		speedValue = value
		speedLabel.Text = "Tốc độ: " .. value .. " / 100"
		applySpeed()
	end
)

RunService.Stepped:Connect(function()
	if not noclipEnabled then return end

	local c = getCharacter()
	if not c then return end

	for _, object in ipairs(c:GetDescendants()) do
		if object:IsA("BasePart") then
			object.CanCollide = false
		end
	end
end)

player.CharacterAdded:Connect(function(character)
	character:WaitForChild("Humanoid")
	character:WaitForChild("HumanoidRootPart")

	task.wait(0.2)

	applyHitbox()
	applySpeed()
end)

-- Kéo menu
local dragging = false
local dragStart
local startPosition

title.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1
		or input.UserInputType == Enum.UserInputType.Touch then

		dragging = true
		dragStart = input.Position
		startPosition = main.Position
	end
end)

UserInputService.InputChanged:Connect(function(input)
	if not dragging then return end

	if input.UserInputType == Enum.UserInputType.MouseMovement
		or input.UserInputType == Enum.UserInputType.Touch then

		local delta = input.Position - dragStart

		main.Position = UDim2.new(
			startPosition.X.Scale,
			startPosition.X.Offset + delta.X,
			startPosition.Y.Scale,
			startPosition.Y.Offset + delta.Y
		)
	end
end)

UserInputService.InputEnded:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1
		or input.UserInputType == Enum.UserInputType.Touch then

		dragging = false
	end
end)
